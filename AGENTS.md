# AGENTS.md

## Repository Routing

- This is the canonical git repository for FinQuery Contracts Card backend/API work.
- When deploying or pushing FinQuery project changes, push to this repo (`patch-ops-llc/finquery-contracts`) on the appropriate branch (default `main`).
- Do not route FinQuery project pushes to personal/umbrella repos like `patch-ops-llc/Zach`.
